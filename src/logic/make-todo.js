class Todo {
    constructor(task, dueDate) {
        this.task = task;
        this.dueDate = dueDate;
    }

    setTitle(task) {
        this.task = task;
    }

    setDueDate(dueDate) {
        this.dueDate = dueDate;
    }
    
}